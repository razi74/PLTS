Requirements::
JDK 1.8 installation.
Maven 3.3.9 installation.
Input file- file_inputs.txt to be available under plts directory
Edit the parking_lot.sh file to point to the JAVA_HOME and update the PATH variable

Usage::
To exit interactive mode type exit on the console.